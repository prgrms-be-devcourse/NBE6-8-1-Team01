spring:
  datasource:
    url: jdbc:postgresql://db.ggookikrmlkkdbgqfymi.supabase.co:5432/postgres
    username: postgres
    password: ${DB_PASSWORD}  # 환경변수로 관리
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000